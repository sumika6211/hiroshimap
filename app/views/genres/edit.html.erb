<h1>ジャンル編集</h1>

<%= form_with model: @genre, url: genre_path, method: :patch do |f| %>
  <div class = "field">
    <%= f.label :"ジャンル名" %>
    <%= f.text_field :name %>
  </div>

  <div class = "actions">
    <%= f.submit "作成する" %>
  </div>
<% end %>
