<h1>ジャンルを作成する</h1>

<%= form_with model: @genre, url: genres_path, method: :post do |f| %>
  <div class = "field">
    <%= f.label :"ジャンル名" %>
    <%= f.text_field :name, placeholder: "ジャンル名を入力してください" %>
  </div>

  <div class = "actions">
    <%= f.submit "作成する" %>
  </div>
<% end %>