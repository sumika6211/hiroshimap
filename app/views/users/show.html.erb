<h1>Users info</h1>

<div>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>
<tr>
  <th>name</th>
  <th><%= @user.name %></th>
</tr>
<tr>
  <th>
    <% if @user.id == current_user.id %>
      <%= link_to edit_user_path(@user.id) do %>
        編集
      <% end %>
    <% end %>
  </th>
  <th></th>
</tr>